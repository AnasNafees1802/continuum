# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# sed backup files from installers
*.bak

# don't commit a test ledger if you run the installer inside this clone
.aicontext/

# Continuum dogfooding: running the installer inside this repo drops these
# machine-local adapter/config/skill artifacts at the root. They are byproducts,
# not product source (the product templates live in adapters/, skill/, templates/, bin/).
/.aicontext/
/.claude/
/.codex/
/.agents/
/.windsurfrules
/CLAUDE.md
/AGENTS.md

# raw logo source (site uses the optimized docs/logo.png + favicon.png)
/docs/continuum_logo.png

# Remotion promo-video SOURCE stays local, never pushed (heavy deps + render output).
# Only the optimized web MP4 in docs/ is committed. Re-render locally from the video/ project.
/video/
